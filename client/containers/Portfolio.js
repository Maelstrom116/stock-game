import React from 'react';
import Holding from './Holding.js';

const Portfolio = (props) => {
  return (
    <div
      style={{
        paddingTop: '50px',
      }}
    >
      <Holding />
    </div>
  );
};

export default Portfolio;
